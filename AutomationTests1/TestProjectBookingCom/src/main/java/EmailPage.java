import org.openqa.selenium.WebDriver;
//page on  which you get after working method in Price Page
public class EmailPage {
    private WebDriver driver;

    public EmailPage(WebDriver driver) {
        this.driver = driver;
    }
}
